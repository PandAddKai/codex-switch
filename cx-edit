#!/usr/bin/env bash
set -euo pipefail

if [[ "${1:-}" == "" || "${1:-}" == "-h" || "${1:-}" == "--help" ]]; then
  cat <<'EOF'
Usage:
  cx-edit <profile>

Shortcut for:
  codex-switch edit <profile>
EOF
  exit 0
fi

exec codex-switch edit "$@"

