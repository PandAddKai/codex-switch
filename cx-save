#!/usr/bin/env bash
set -euo pipefail

if [[ "${1:-}" == "" || "${1:-}" == "-h" || "${1:-}" == "--help" ]]; then
  cat <<'EOF'
Usage:
  cx-save <profile>

Shortcut for:
  codex-switch save <profile>
EOF
  exit 0
fi

exec codex-switch save "$1"

