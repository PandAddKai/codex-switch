#!/usr/bin/env bash
set -euo pipefail

if [[ "${1:-}" == "-h" || "${1:-}" == "--help" ]]; then
  cat <<'EOF'
Usage:
  cx-list

Shortcut for:
  codex-switch list
EOF
  exit 0
fi

exec codex-switch list

